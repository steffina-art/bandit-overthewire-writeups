
## Level 7 — grep for a Keyword

This level handed me a big file, `data.txt`, and said the password sat on the same line as the word `millionth`.

```bash
grep millionth data.txt
```

`grep` printed the whole line, word and password together, and I just grabbed the second token.
 
