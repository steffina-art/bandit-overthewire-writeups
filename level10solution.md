## Level 10 — Base64 Decoding

`data.txt` here was base64-encoded. Decoding it is one command:

```bash
base64 -d data.txt
```
d refers to decoding the encoded text
Password popped right out in plain text.

 
